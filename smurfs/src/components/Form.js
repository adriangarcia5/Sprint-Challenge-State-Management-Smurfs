import React from 'react';

const Form = props => {
    console.log('smurfs')
    return (<>
    <form>
        <input></input>
        <button>New Blue Thing</button>
        </form>

    </>
    )}

export default (Form); 